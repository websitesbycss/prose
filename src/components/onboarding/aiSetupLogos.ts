// Static asset imports — Vite bundles these like any other image (the
// project's Vite root is the repo root, so `resources/` is reachable from
// here). Replace the files at resources/logos/*.png with the real logos;
// nothing else needs to change.
import ollamaLogo from '../../../resources/logos/ollama.png'
import openaiLogo from '../../../resources/logos/openai.png'
import anthropicLogo from '../../../resources/logos/anthropic.png'
import geminiLogo from '../../../resources/logos/gemini.png'

export { ollamaLogo, openaiLogo, anthropicLogo, geminiLogo }
